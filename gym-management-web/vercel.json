{
  "buildCommand": "npm install && npm run build",
  "outputDirectory": "dist/gym-management-web",
  "env": {
    "API_URL": "@gym_api_url"
  },
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
